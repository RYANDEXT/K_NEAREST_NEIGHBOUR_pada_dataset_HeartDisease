# -*- coding: utf-8 -*-
"""KNN_Pertemuan6.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1h-fzY7FEGVxN5FIwvCLdgro6YE5yVMxx
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.neighbors import KNeighborsClassifier
from sklearn.metrics import confusion_matrix
from matplotlib.colors import ListedColormap

dataset = pd.read_csv("Heart_disease_cleveland_new.csv")

dataset.head()

x = dataset.drop("target", axis =1)
y = dataset["target"]

x_train, x_test, y_train, y_test = train_test_split(x,y, test_size = 0.25, random_state =0)

sc = StandardScaler()
x_train = sc.fit_transform(x_train)

KNN = KNeighborsClassifier(n_neighbors=13, metric='euclidean', p=2)
KNN.fit(x_train, y_train)

y_pred = KNN.predict(x_test)

cm = confusion_matrix(y_test, y_pred)
print(cm)

